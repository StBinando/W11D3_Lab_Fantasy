package characters.mages;

import characters.Fighter;
import characters.Mage;

public class Warlock extends Mage {
    public Warlock(int healthPoints, String name) {
        super(healthPoints, name);
    }
}